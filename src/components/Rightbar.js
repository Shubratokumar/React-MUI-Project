import React from 'react';
import { Box } from '@mui/material';

const Rightbar = () => {
    return (
        <Box bgcolor="red">
            Rightbar
        </Box>
    );
};

export default Rightbar;