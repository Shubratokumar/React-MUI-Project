import React from 'react';
import { Box } from '@mui/material';

const Sidebar = () => {
    return (
        <Box bgcolor="purple">
            Sidebar
        </Box>
    );
};

export default Sidebar;